{
  "name": "snaredusk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "playtest:smoke": "node scripts/playtest-smoke.mjs",
    "sprites:placeholders": "node scripts/generate-creature-placeholders.mjs",
    "sprites:environment": "node scripts/generate-environment-placeholders.mjs",
    "audio:sfx": "node scripts/generate-sfx-placeholders.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "pixi.js": "^8.9.2"
  }
}
